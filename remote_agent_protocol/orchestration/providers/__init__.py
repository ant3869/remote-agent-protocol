"""ModelProvider implementations.

LocalProvider (Ollama) and CopilotProvider (the official
``github-copilot-sdk``). See ``providers.base.ModelProvider`` for the
interface every provider implements -- that ABC is the extension point; no
separate stub subclass is needed to prove it's extensible.
"""
